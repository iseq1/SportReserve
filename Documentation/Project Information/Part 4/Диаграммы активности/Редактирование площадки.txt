@startuml
partition "Администратор" {
(*) --> "Заходит в приложение"
--> "Авторизуется"
--> "Открывает страницу утрапвления площадками"
if "" then
  --> [Нет доступных площадок](*)
else
  --> "Находит нужную площадку"
endif
--> "Открывает страницу площадки"

--> "Нажимает кнопку 'Редактировать'"
--> "Попадает на окно редактирования площадки"
--> "Изменяет необходимую информацию"
--> "Нажимате кнопку сохранить"
if "" then
  -->[Новые данные содержат ошибки]"Вывод инофрмации об ошибке"
  --> "Попадает на окно редактирования площадки"
else
  -->[Данные сохранены]"Переход на обновленную страницу площадки"
endif
if "" then
  --> [Есть заказы на эту площадку] "Отправить пользователям уведомление об изменениях"
  --> (*)
else
  --> (*)
endif

}
@endumls