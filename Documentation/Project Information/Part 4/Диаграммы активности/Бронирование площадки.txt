@startuml
partition "Пользователь" {
(*) --> "Заходит в приложение"
--> "Авторизуется"
--> "Попадает на страницу выбора настроек"
--> "Выбирает тип сопрта"
--> "Выбирает нужное время"
--> "Нажимает поиск"
if "" then
  -->[Нет доступных площадок]"Попадает на страницу выбора настроек"
else
  --> "Открывается список доступных площадок"
endif
--> "Выбирает площадку из списка"
--> "Нажмает бронировать"
--> "Попадает на окно с инофрмацией\n о бронировании и кнопками\n подтвердить/отменить"
if "" then
  -->[Отмена]"Открывается список доступных площадок"
else
  -->[Подтверждение]"Переход на страницу бронирования"
endif
--> "Нажатие кнопки опалаты"
--> "Переход во внешний платежный сервис"
if "" then
  -->[Оплата успешна]"Переход на страницу оплаченного бронирования"
  --> (*)
else
  -->[Оплата неуспешна]"Вывод сообщения об ошибке"
  --> "Переход на страницу бронирования"
endif
}
@enduml